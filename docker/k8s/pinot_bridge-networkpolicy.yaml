apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: pinot_bridge
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/pinot_bridge: "true"
  podSelector:
    matchLabels:
      io.kompose.network/pinot_bridge: "true"
